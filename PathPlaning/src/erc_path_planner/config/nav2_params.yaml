planner_server:
  ros__parameters:

controller_server:
  ros__parameters:

bt_navigator:
  ros__parameters:

global_costmap:
  global_costmap:
    ros__parameters:

local_costmap:
  local_costmap:
    ros__parameters: